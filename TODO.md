 根据探索结果，这是当前项目状态和下一步建议：

  当前 zpencode 实现状态
  ┌────────────┬──────┬──────────────────────────────────┐
  │    功能    │ 状态 │               说明               │
  ├────────────┼──────┼──────────────────────────────────┤
  │ TUI 界面   │ ✅   │ vaxis 框架，消息显示、输入、命令 │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 多提供商   │ ✅   │ Anthropic/OpenAI/DeepSeek/Ollama │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 配置管理   │ ✅   │ ~/.config/zpencode/config.json   │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 交互式菜单 │ ✅   │ prompt.zig 实现                  │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 流式输出   │ ⚠️   │ DeepSeek 支持，其他需完善        │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 状态栏     │ ❌   │ 未实现                           │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 多行输入   │ ❌   │ 未实现                           │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 代码高亮   │ ❌   │ 未实现                           │
  ├────────────┼──────┼──────────────────────────────────┤
  │ 会话持久化 │ ❌   │ 未实现                           │
  └────────────┴──────┴──────────────────────────────────┘
  建议的下一步

  按实现复杂度排序：

  1. 状态栏（低难度） - 最快见效
  - 底部显示：Provider: anthropic | Model: claude-sonnet | Tokens: 0
  - 只需修改 tui/app.zig 的 render 函数

  2. 多行输入（低难度）
  - Shift+Enter 换行，Enter 发送
  - 修改按键处理逻辑

  3. 完善流式输出（中难度）
  - 让 Anthropic/OpenAI 也支持实时显示
  - 修改 providers/client.zig

  4. 会话持久化（中难度）
  - SQLite 存储历史会话
  - 新建 storage/ 模块

  5. Markdown/代码高亮（高难度）
  - 需要集成 tree-sitter 或自己解析

  你想从哪个开始？我推荐先做状态栏，能快速提升使用体验。
